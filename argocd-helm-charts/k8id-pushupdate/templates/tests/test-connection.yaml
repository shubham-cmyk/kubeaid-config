apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pushupdate-service.fullname" . }}-test-connection"
  labels:
    {{- include "pushupdate-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pushupdate-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
