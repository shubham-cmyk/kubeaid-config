apiVersion: v1
kind: Service
metadata:
  name: {{ include "pushupdate-service.fullname" . }}
  labels:
    {{- include "pushupdate-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "pushupdate-service.selectorLabels" . | nindent 4 }}
