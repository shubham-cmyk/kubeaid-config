---
appConfig:
  argocd:
    url: "argocd-server.argocd.svc.cluster.local:443"
  kubeaidConfig:
    clusterName: mytestcluster7
  obmondoAPI:
    url: https://api-beta.obmondo.com/api
  kubeaidUpdate:
    enabled: true
    checkInterval: 5m

deployment:
  image:
    repository: harbor.obmondo.com/obmondo/kubeaid-agent
    tag: "201"
    pullPolicy: Always
