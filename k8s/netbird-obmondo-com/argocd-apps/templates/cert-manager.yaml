apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    kubeaid.io/version: master
    kubeaid.io/managed-by: kubeaid
    kubeaid.io/sync-order: "10"
  name: cert-manager
  namespace: argocd
spec:
  destination:
    namespace: cert-manager
    server: https://kubernetes.default.svc
  project: kubeaid
  sources:
    - repoURL: https://github.com/ashish1099/kubeaid.git
      path: argocd-helm-charts/cert-manager
      targetRevision: master
      helm:
        valueFiles:
          - $values/k8s/netbird-obmondo-com/argocd-apps/values-cert-manager.yaml
    - repoURL: ssh://git@gitea.obmondo.com:2223/EnableIT/kubeaid-config-enableit.git
      targetRevision: HEAD
      ref: values
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
