apiVersion: v1
kind: Secret
metadata:
  labels:
    app.kubernetes.io/component: grafana
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: kube-prometheus
    app.kubernetes.io/version: 9.5.1
  name: grafana-config
  namespace: monitoring
stringData:
  grafana.ini: |
    [analytics]
    check_for_updates = false
    [auth]
    disable_login_form = false
    disable_signout_menu = false
    signout_redirect_url = https://keycloakx.kam.obmondo.com/auth/realms/Obmondo/protocol/openid-connect/logout?redirect_uri=https://grafana.kam.obmondo.com
    [auth.generic_oauth]
    allow_sign_up = true
    api_url = https://keycloakx.kam.obmondo.com/auth/realms/Obmondo/protocol/openid-connect/userinfo
    auth_url = https://keycloakx.kam.obmondo.com/auth/realms/Obmondo/protocol/openid-connect/auth
    client_id = grafana
    enabled = true
    name = Keycloak
    role_attribute_path = contains(not_null(roles[*],''), 'Admin') && 'Admin' || contains(not_null(roles[*],''), 'Editor') && 'Editor' || contains(not_null(roles[*],''), 'Viewer') && 'Viewer'|| ''
    scopes = openid profile email
    token_url = https://keycloakx.kam.obmondo.com/auth/realms/Obmondo/protocol/openid-connect/token
    [date_formats]
    default_timezone = UTC
    [server]
    root_url = https://grafana.kam.obmondo.com
type: Opaque
