apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: grafana
  namespace: monitoring
spec:
  rules:
  - host: grafana.obmondo.com
    http:
      paths:
      - backend:
          service:
            name: grafana
            port:
              name: http
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - grafana.obmondo.com
    secretName: kube-prometheus-grafana-tls
