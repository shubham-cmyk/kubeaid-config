mail:
    config:
        general:
            TZ: 'Europe/Copenhagen'
            RELAYHOST: 'mail.obmondo.com'
            ALLOWED_SENDER_DOMAINS: 'obmondo.com enableit.dk'
            LOG_FORMAT: 'plain'  # Change to JSON to get json log output
