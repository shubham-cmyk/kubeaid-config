---
argo-cd:
  dex:
    enabled: false
  repoServer:
    replicas: 2
    autoscaling:
      enabled: false
  configs:
    secret:
      createSecret: false     # managed by sealed-secrets now
  server:
    rbacConfig:
      policy.csv: |
        g, ArgoCDAdmins, role:admin
        g, ArgoCDDevs, role:readonly
        g, deepak@obmondo.com, role:admin
        g, divyam@obmondo.com, role:admin
        g, akshat@obmondo.com, role:admin
        g, vivek@obmondo.com, role:admin
      scopes: '[groups, email]'
    extraArgs:
      - --insecure
    config:
      url: https://argocd.kbm.obmondo.com
      oidc.config: |
        name: Keycloak
        issuer: https://keycloakx.kam.obmondo.com/auth/realms/Obmondo
        clientID: argocd
        clientSecret: $oidc.keycloak.clientSecret
        requestedScopes: ["openid", "profile", "email", "groups"]
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: traefik-cert-manager
        # necessary for bare-metal only - for unknown reason.
        cert-manager.io/cluster-issuer: letsencrypt
      hosts:
      - argocd.kbm.obmondo.com
      tls:
      - secretName: argocd-ssl
        hosts:
        - argocd.kbm.obmondo.com
