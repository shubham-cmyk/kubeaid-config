issuer:
  enabled: true
  issuerEmail: klavs@obmondo.com
  production: true
  secret: kbm-letsencrypt-key
  solvers:
    - type: dns
      cloudProvider: cloudflare
      cloudProviderSecret: cloudflare-api-token-secret
      dnsNames:
        - obmondo.com
    - type: http
