apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: errbot
  namespace: argocd
spec:
  destination:
    namespace: mattermost
    server: https://kubernetes.default.svc
  project: default
  source:
    path: argocd-helm-charts/errbot
    repoURL: https://gitlab.enableit.dk/kubernetes/k8id.git
    targetRevision: HEAD
    helm:
      valueFiles:
      - values.yaml
      - /tmp/https___gitlab.enableit.dk_kubernetes_kubernetes-config-enableit/k8s/kbm.obmondo.com/argocd-apps/values-errbot.yaml
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    - ApplyOutOfSyncOnly=true
