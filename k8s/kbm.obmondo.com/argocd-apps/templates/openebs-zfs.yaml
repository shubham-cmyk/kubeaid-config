apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openebs-zfs
  namespace: argocd
spec:
  destination:
    namespace: openebs-zfs
    server: 'https://kubernetes.default.svc'
  source:
    path: argocd-helm-charts/openebs-zfs
    repoURL: 'https://gitlab.enableit.dk/kubernetes/k8id.git'
    targetRevision: HEAD
    helm:
      valueFiles:
        - values.yaml
        - /tmp/https___gitlab.enableit.dk_kubernetes_kubernetes-config-enableit/k8s/kbm.obmondo.com/argocd-apps/values-openebs-zfs.yaml
  project: default
  syncPolicy:
    automated: null
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true