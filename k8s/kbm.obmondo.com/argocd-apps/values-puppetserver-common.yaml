puppetserver:
  storage:
    storageClass: rook-ceph-block-inodes-highfilecount
  puppetserver:
    puppeturl: "https://gitea.obmondo.com/EnableIT/puppet.git"
    persistence:
      code:
        size: 5Gi
