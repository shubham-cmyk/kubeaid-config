apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: metallb
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    path: argocd-helm-charts/metallb
    repoURL: 'https://gitlab.enableit.dk/kubernetes/kubernetes-config-enableit.git'
    targetRevision: HEAD
    helm:
      valueFiles:
      - values.yaml
      - ../../kam.obmondo.com/argocd_apps/values-metallb.yaml
  syncPolicy:
    syncOptions:
    - ApplyOutOfSyncOnly=true
    - CreateNamespace=true
