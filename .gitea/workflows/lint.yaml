name: K8id config lint pipeline
run-name: Lint CI
"on":
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  markdownlint:
    runs-on: ubuntu-latest
    name: Markdown Lint
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: markdownlint
        uses: https://gitea.obmondo.com/Obmondo/gitea-actions/.gitea/actions/markdownlint-cli2-action@main
        with:
          config: .markdownlint-cli2.yaml
          globs: '**/*.md'

  yamllint:
    name: Yaml Lint
    runs-on: ubuntu-latest
    container:
      image: harbor.obmondo.com/obmondo/kubeaid-ci:1.2.0
    steps:
    - name: checkout
      uses: actions/checkout@v2

    - name: yamllint
      run: |
        yamllint --strict --config-file .yamllint .
